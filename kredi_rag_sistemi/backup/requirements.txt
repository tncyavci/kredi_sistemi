# Kredi RAG Sistemi - CPU Optimized Requirements
# Gereksiz paketler temizlendi, sadece gerekli olanlar bırakıldı
# Tablo işleme kütüphaneleri eklendi

# Core dependencies
streamlit>=1.28.0
python-dotenv>=1.0.0
psutil>=5.9.0

# LLM and embeddings - CPU optimized
llama-cpp-python>=0.2.38
sentence-transformers>=2.2.2
huggingface-hub>=0.20.2
transformers>=4.30.2

# Vector database - CPU only
faiss-cpu>=1.7.4
chromadb>=0.4.18

# PDF processing - essential + table extraction
PyPDF2>=3.0.1
pdfplumber>=0.10.3
PyMuPDF>=1.23.8

# Table extraction libraries
camelot-py[cv]>=0.11.0
tabula-py>=2.8.2
openpyxl>=3.1.2

# Image processing for table extraction (CPU only)
opencv-python-headless>=4.8.1.78
Pillow>=10.0.1

# Text processing
langchain>=0.1.4
tiktoken>=0.5.0

# Data processing
pandas>=2.0.3
numpy>=1.24.3

# Web framework
fastapi>=0.104.1
uvicorn>=0.24.0

# Utilities
requests>=2.31.0
tqdm>=4.65.0
python-multipart>=0.0.6

# Development
pytest>=7.4.3

# Note: GPU bağımlılıkları kaldırıldı (torch, easyocr)
# Note: Kullanılmayan vektör DB'ler kaldırıldı (redis, pymilvus, pinecone)
# Note: OCR paketleri CPU-only versiyonları kullanılıyor
# Note: Tablo işleme kütüphaneleri eklendi (camelot, tabula) 